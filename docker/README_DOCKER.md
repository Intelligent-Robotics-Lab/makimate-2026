#Docker Read Me

### Requirements

- Docker (Download for native OS)
- Docker Desktop


### Starting the dev environment

```bash
docker compose -f docker/docker-compose.yml build base
docker compose -f docker/docker-compose.yml run base
```

You can check if a container is running within Docker Desktop


